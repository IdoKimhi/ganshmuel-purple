version: '3.9'

networks:
  purple_network:
    driver: bridge

services:

  weight-db:
    extends:
      file: ../weight/docker-compose.yml
      service: weight-db
    networks:
      - purple_network
    network_mode: ""  # איפוס

  weight-app:
    extends:
      file: ../weight/docker-compose.yml
      service: weight-app
    networks:
      - purple_network
    network_mode: ""  # איפוס

  billing-db:
    extends:
      file: ../billing/docker-compose.yml
      service: billing-db
    networks:
      - purple_network
    network_mode: ""  # איפוס

  billing:
    extends:
      file: ../billing/docker-compose.yml
      service: billing
    networks:
      - purple_network
    network_mode: ""  # איפוס
